<?xml version="1.0"?>
<launch>
  <!-- send robot urdf to ROS param server -->
  <include file="$(find staubli_tx60_gazebo)/launch/load_tx60.launch" />

  <arg name="start_gazebo" default="true" />
  <arg name="start_rviz" default="false" />
  <include file="$(find-pkg-share gazebo_ros)/launch/gazebo.launch.py">
    <!--arg
    name="paused" value="true"/>
    <arg name="use_sim_time" value="true"/>
    <arg name="gui" value="true"/>
    <arg name="recording" value="false"/>
    <arg name="debug" value="false"/>
    <arg name="verbose" value="true"/-->
  </include>

  <!-- push robot_description to factory and spawn robot in gazebo -->
  <node name="spawn_gazebo_model" pkg="gazebo_ros" exec="spawn_entity.py"
    output="screen" />

  <!-- convert joint states to TF transforms for rviz, etc -->
  <node name="robot_state_publisher" pkg="robot_state_publisher" exec="robot_state_publisher" />

  <!-- init and start Gazebo ros_control interface -->
  <include file="$(find staubli_tx60_gazebo)/launch/tx60_control.launch.xml" />

</launch>